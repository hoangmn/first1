import 'package:flutter/material.dart';

class NewsIcon extends StatelessWidget {
  const NewsIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return InkWell(
     
    );
  }
}